import { Get, Post, Put, Delete, Controller, Body, Req ,Res} from '@nestjs/common';
import { CreateItemDto } from './dto/create-item.dto';
import {Request,Response} from 'express';
@Controller('items')
export class ItemsController {
  @Get()
  findAllItems(@Req()req:Request,@Res() res:Response):Response{
      console.log(req.url)
      return res.send({name:'Hllo items'})
    // return [{name:'Hllo items'}]
  }

  @Post()
  createItem(@Body() createItemDto: CreateItemDto): string {
    return `name ${createItemDto.name} - descriptionc ${createItemDto.description} - qty ${createItemDto.qty}`;
  }
}
